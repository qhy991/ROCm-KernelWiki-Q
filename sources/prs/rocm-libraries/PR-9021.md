---
id: pr-rocm-libraries-9021
type: source-pr
repo: ROCm/rocm-libraries
pr: 9021
title: 'fix(stinkytofu): tolerate non-mbcs bytes in vcvarsall.bat output'
author: KKyang
date: '2026-07-03'
url: https://github.com/ROCm/rocm-libraries/pull/9021
source_category: upstream-code
architectures:
- cdna2
- cdna3
- cdna4
tags:
- rocm-kernel
- rocm
- rocm-libraries
kernel_types:
- attention
languages:
- python
captured_at: '2026-07-21'
status: merged
inclusion_reason: may contain relevant infrastructure changes
confidence: source-reported
description: '- \`invoke build\` crashes in \`_setup_msvc_env()\` on a Windows machine
  whose active code page doesn''t match what VS''s tooling assumes (e.g. a JIS/Shift-JIS
  sy'
---

# fix(stinkytofu): tolerate non-mbcs bytes in vcvarsall.bat output

Merged PR #9021 in [ROCm/rocm-libraries](https://github.com/ROCm/rocm-libraries/pull/9021).

**Author:** KKyang
**Merged:** 2026-07-03

## Description

> - \`invoke build\` crashes in \`_setup_msvc_env()\` on a Windows machine whose active code page doesn't match what VS's tooling assumes (e.g. a JIS/Shift-JIS system locale with an English-language VS Build Tools install): an undecodable byte in \`vcvarsall.bat\`'s banner output aborts the \`mbcs\`-decoded read mid-stream, leaving \`result.stdout\` as \`None\` and crashing the following \`.splitlines()\` call.

## Changed Files

- `shared/stinkytofu/tasks.py` (+24/-5)
- `shared/stinkytofu/tests/test_tasks.py` (+46/-0)

See the PR for full details including code changes and review discussion.

## References

- [PR #9021](https://github.com/ROCm/rocm-libraries/pull/9021)
