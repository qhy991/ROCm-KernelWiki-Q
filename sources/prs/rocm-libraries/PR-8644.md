---
id: pr-rocm-libraries-8644
type: source-pr
repo: ROCm/rocm-libraries
pr: 8644
title: '[CK][CI] Expand other stages to use healthy-node retry logic.'
author: brockhargreaves-amd
date: '2026-06-29'
url: https://github.com/ROCm/rocm-libraries/pull/8644
source_category: upstream-code
architectures:
- cdna2
- cdna3
- cdna4
tags:
- rocm
- rocm-libraries
- rocm-kernel
kernel_types: []
languages: []
captured_at: '2026-07-21'
status: merged
inclusion_reason: may contain relevant infrastructure changes
confidence: source-reported
techniques:
- persistent-kernel
subproject: composablekernel
description: "CI stages bound to a single node via the declarative `agent{ label }`\
  \ cannot recover when that node is unhealthy \u2014 a `NodeFault` (e.g. an exhausted\
  \ `gitNetRetry"
---

# [CK][CI] Expand other stages to use healthy-node retry logic.

Merged PR #8644 in [ROCm/rocm-libraries](https://github.com/ROCm/rocm-libraries/pull/8644).

**Author:** brockhargreaves-amd
**Merged:** 2026-06-29

## Description

> CI stages bound to a single node via the declarative `agent{ label }` cannot recover when that node is unhealthy — a `NodeFault` (e.g. an exhausted `gitNetRetry` after a persistent `Could not resolve host: github.com`, or a missing GPU) just fails the whole build. The existing `runOnHealthyNode` wrapper already reroutes such faults to a different node, but only the "Build CK and run Tests" stages used it. This PR brings the remaining node-bound stages under that wrapper so a bad node reroutes in

## Changed Files

- `projects/composablekernel/Jenkinsfile` (+133/-87)
- `projects/composablekernel/groovy/vars/ck.groovy` (+14/-5)

See the PR for full details including code changes and review discussion.

## References

- [PR #8644](https://github.com/ROCm/rocm-libraries/pull/8644)
